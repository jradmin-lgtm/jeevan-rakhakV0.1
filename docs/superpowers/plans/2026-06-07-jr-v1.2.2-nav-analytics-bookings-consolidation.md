# Jeevan Rakshak v1.2.2 — Client nav finalize · dashboard analytics · bookings consolidation · newest-first

**Date:** 2026-06-07 · **Builds on:** v1.2.1 (`7d0af7d`, live) · admin-web + backend only — **no APK** · branch `prod` · additive, fail-loud DDL, zero regression.

## Goals
1. **Hospital portal final nav = exactly 5:** **Dashboard · Drivers · Rides · Feedbacks · Help & Support.** Rename History→Rides; split Support→Feedbacks (soft feedback) + Help & Support (issues admin resolves) via a `category` field on the one ticket entity.
2. **Hospital Dashboard analytics board** (scoped, client-POV KPIs).
3. **Admin: fold Cancellations into Bookings** — status filters + inline cancellation reason/remarks; remove the standalone `/cancellations` nav/page (keep the data + endpoint).
4. **Newest-first everywhere** in admin lists.
5. Security/RBAC unchanged — all hospital queries stay `hid`-scoped; no `portal_*` ever leaves admin.

---

## Task 1 — Ticket `category` (Feedbacks vs Help & Support)
**Files:** `packages/db/src/schema.ts`, `services/api-server/src/main.ts`, `services/api-server/src/routes/hospital.ts`, `services/api-server/src/routes/admin.ts`.
- [ ] schema: add `category: text("category").default("ISSUE").notNull()` to `supportTickets` (`'FEEDBACK' | 'ISSUE'`).
- [ ] main.ts DDL (v1.2.1 block): `ALTER TABLE support_tickets ADD COLUMN IF NOT EXISTS category text NOT NULL DEFAULT 'ISSUE'`; bump migrate log to v1.2.2.
- [ ] `POST /hospital/tickets`: accept `category` in (`FEEDBACK`,`ISSUE`), default `ISSUE`; store it.
- [ ] `GET /hospital/tickets` + `GET /admin/tickets`: return `category`; both accept optional `?category=FEEDBACK|ISSUE` filter (keep existing `?status`).
- [ ] Admin open-count (the badge/analytics stat): count `status='OPEN' AND category='ISSUE'` (feedback isn't an actionable "open" item). Keep PATCH resolve working for both.
- [ ] Verify `pnpm --filter @jr/db build && pnpm --filter @jr/api-server typecheck`. Commit `v1.2.2: ticket category (FEEDBACK|ISSUE) — splits Feedbacks vs Help & Support`.

## Task 2 — Hospital analytics endpoint (scoped)
**Files:** `services/api-server/src/routes/hospital.ts`.
- [ ] `GET /api/v1/hospital/analytics` (preHandler requireHospital; `hid` from claim). All queries `WHERE dest_hospital_id = hid`. Return:
  - `incomingNow` (status IN ACCEPTED/ARRIVED/PICKED_UP), `preparingNow` (hospital_ack_at IS NOT NULL AND status != ARRIVED-at-hospital/COMPLETED — i.e. acknowledged + still en route), `arrivalsToday` (COMPLETED today), `sosToday` (is_sos AND created today), `ridesTotal`.
  - `avgPickupToHospitalMin` — avg(EXTRACT(EPOCH FROM (completed_at - picked_up_at))/60) over COMPLETED in last 30d (null if none).
  - `ambulances` — reuse `/hospital/me` counts (total/online/onTrip) or recompute here.
  - `last7Days` — array `[{day, count}]` (rides/day, last 7 days, created_at).
  - `byEmergencyType` — `[{type, count}]` over last 30d.
  - `statusMix` — `{completed, active, cancelled}`.
  Use timezone Asia/Kolkata for "today" (`(now() AT TIME ZONE 'Asia/Kolkata')::date`), matching the admin dashboard's IST convention (confirm against admin.ts date math).
- [ ] Verify typecheck. Commit `v1.2.2: GET /hospital/analytics (scoped client KPIs)`.

## Task 3 — Admin bookings: status filter + inline cancellation details
**Files:** `services/api-server/src/routes/admin.ts` (GET /admin/bookings ~L171), `apps/admin-web/app/(admin)/bookings/BookingsList.tsx`, `apps/admin-web/app/(admin)/bookings/page.tsx`.
- [ ] `GET /admin/bookings`: accept optional `?status=` (one of the booking statuses) → add to WHERE; keep `desc(createdAt)`. For cancelled rows, LEFT JOIN `booking_cancellations` (latest per booking) to include `cancel_reason`, `cancel_remarks`, `cancel_outcome` in the row (null for non-cancelled). (Use a raw pgClient query if the join is awkward in Drizzle; keep response keys additive.)
- [ ] `BookingsList.tsx`: add a **status filter** bar (All · Requested · Accepted · Arrived · Picked up · Completed · **Cancelled** · Timed out). For **Cancelled** rows show the reason (humanized) + remarks inline (or in the row's detail). Keep the existing realtime poll. Newest-first (already).
- [ ] Verify `pnpm --filter @jr/api-server typecheck && pnpm --filter @jr/admin-web typecheck`. Commit `v1.2.2: admin bookings status-filter + inline cancellation reason/remarks`.

## Task 4 — Remove standalone Cancellations section (folded into Bookings)
**Files:** `apps/admin-web/app/(admin)/layout.tsx`, `apps/admin-web/app/(admin)/cancellations/*`.
- [ ] Remove the `Cancellations` nav link from `(admin)/layout.tsx`. Delete the `(admin)/cancellations/` page + list (data now surfaced via Bookings → Cancelled filter). Keep the backend `GET /admin/cancellations` endpoint (harmless; the bookings join uses booking_cancellations directly).
- [ ] Verify `pnpm --filter @jr/admin-web typecheck`. Commit `v1.2.2: fold Cancellations into Bookings (remove standalone section)`.

## Task 5 — Newest-first sweep (admin lists)
**Files:** `services/api-server/src/routes/admin.ts` + the admin-web list components.
- [ ] Audit each admin list endpoint + its web renderer: Bookings (`desc(createdAt)` ✓), Users (`desc(createdAt)` ✓), Drivers (currently `desc(lastSeenAt)` — keep as activity-recency, but confirm the web doesn't re-sort to break it), Feedback, Hospitals (alphabetical — leave), Tickets (`desc(created_at)` ✓). For any list NOT newest-first by created/updated (and where newest-first is the right semantic), set `ORDER BY created_at DESC`. Confirm no web component re-sorts ascending. Document findings in the commit.
- [ ] Verify typecheck. Commit `v1.2.2: ensure newest-first ordering across admin lists`.

## Task 6 — Hospital portal: final 5-section nav (Rides + Feedbacks/Help split)
**Files:** `apps/admin-web/app/(hospital)/layout.tsx`, rename `h/history` → `h/rides`, new `h/feedbacks` + relabel `h/support` → `h/help`, `RaiseTicketForm.tsx`.
- [ ] Nav = **Dashboard (`/h`) · Drivers (`/h/drivers`) · Rides (`/h/rides`) · Feedbacks (`/h/feedbacks`) · Help & Support (`/h/help`)**.
- [ ] Rename the History route folder `h/history` → `h/rides` (update the `HospitalHistoryLive` import/label to "Rides"); update the dashboard's links if any point to `/h/history`.
- [ ] `RaiseTicketForm`: add a `category` prop/选 (FEEDBACK | ISSUE). Feedbacks page raises `FEEDBACK` (general or contextual), Help & Support raises `ISSUE`. Contextual "Raise concern" on driver/ride cards → default `ISSUE` (a concern), with a toggle to switch to feedback. POST includes `category`.
- [ ] `h/feedbacks/page.tsx` (+ Live): list this hospital's `category=FEEDBACK` tickets (GET `/hospital/tickets?category=FEEDBACK`) + a "Leave feedback" button (RaiseTicketForm category=FEEDBACK).
- [ ] `h/help/page.tsx` (+ Live): list `category=ISSUE` tickets with status (Open/Resolved) + a "Raise an issue" button (category=ISSUE). (This is the renamed Support page.)
- [ ] Verify `pnpm --filter @jr/admin-web typecheck`. Commit `v1.2.2: hospital portal final nav — Rides + Feedbacks/Help & Support split`.

## Task 7 — Hospital Dashboard analytics board
**Files:** `apps/admin-web/app/(hospital)/h/HospitalDashboardLive.tsx` (+ a `HospitalKpis.tsx` if cleaner).
- [ ] Above the live incoming queue, render a KPI header consuming `GET /api/hospital-proxy/api/v1/hospital/analytics` (10s poll + socket refetch; clear on unmount):
  - KPI cards: **Incoming now · Preparing now · Arrivals today · Avg pickup→hospital · Ambulances online (X/Y) · SOS today**.
  - **7-day rides** mini bar chart (CSS bars, no chart lib — admin-web has no chart dep).
  - **Case mix by emergency type** (horizontal bars / chips) + **Status mix** (completed/active/cancelled).
  - Clean, hospital-branded, responsive; reuse the portal's card styles.
- [ ] Verify `pnpm --filter @jr/admin-web typecheck`. Commit `v1.2.2: hospital dashboard analytics board (KPIs + 7-day trend + case mix)`.

## Task 8 — Admin Help & Support: category-aware
**Files:** `apps/admin-web/app/(admin)/support/SupportTicketsList.tsx` (+ page), `(admin)/layout.tsx` badge.
- [ ] Add a **category filter** (All · Issues · Feedback) to the admin support list; show the category per row. The open-ticket badge already counts OPEN issues (Task 1). Mark-resolved works for both.
- [ ] Verify typecheck. Commit `v1.2.2: admin Help & Support category-aware (Issues vs Feedback)`.

## Task 9 — Version + check
- [ ] admin-web `(admin)` footer + `(hospital)` footer → `v1.2.2`. No app bump.
- [ ] `pnpm check` whole repo green. Commit `v1.2.2: footers + whole-repo typecheck`.

---
## Verification (adversarial)
- **SECRET-LEAK/RBAC:** `/hospital/analytics` + ticket category endpoints scope to `hid` only; cross-hospital still 404/empty; no `portal_*` exposure; analytics counts only this hospital's rides.
- **WIRING:** 5-nav resolves (Rides/Feedbacks/Help routes exist); analytics endpoint has a caller; bookings status-filter hits the API; removed Cancellations nav has no dangling link/import; category split round-trips (raise FEEDBACK → Feedbacks tab + admin Feedback filter; raise ISSUE → Help & Support + badge).
- **NO-REGRESSION + BOOKING-ID:** existing bookings list (no filter) unchanged; `#displayId` still everywhere (no UUID); v1.2.1 live queue/driver-card/ride-card intact; default `/hospital/tickets` still returns all.
- **SORT:** Bookings/Users/Drivers/Tickets render newest-first.
- **TIMER:** new analytics poll + any new list poll cleared on unmount.
- `pnpm check` 10/10.

## Ship (gated): push `prod` → Render deploy both (verify commit==HEAD, `[migrate] schema v1.2.2 ready`, /health) → Vercel auto. No APK.
